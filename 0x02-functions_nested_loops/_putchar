#include <unistd.h>


/**
* _putchar - wriite the character c to stdout
* @c: the chracter to print
*
* Return: on success 1.
* on error: returned -1, and error is set approximately
*/
int _putchar(char c)
{
return (write(1, &c, 1));
}
