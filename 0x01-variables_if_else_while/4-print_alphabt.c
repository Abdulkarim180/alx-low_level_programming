#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: always (success/correct)
 */
int main(void)
{
	char alphabets;

	for (alphabets = 'a'; alphabets <= 'z'; alphabets++)
	{
		if (alphabets == 'q')
			continue;

		else if (alphabets == 'e')
			continue;
	}
		putchar('\n')
		return (0);
}
